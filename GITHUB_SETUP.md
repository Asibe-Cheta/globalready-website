# GitHub Repository Setup Instructions

## After creating the GitHub repository:

Run these commands in your terminal (replace YOUR_USERNAME and YOUR_REPO_NAME):

```bash
cd /Users/justicechetachukwuasibe/Desktop/globalready-website

# Add the remote repository
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Push to GitHub
git push -u origin main
```

## Or if you want to use SSH (if you have SSH keys set up):

```bash
git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO_NAME.git
git push -u origin main
```

## Commands already completed:

✅ Git repository initialized
✅ All files staged
✅ First commit created

## Next steps:

1. Create the GitHub repository (see instructions above)
2. Add the remote and push (commands above)

